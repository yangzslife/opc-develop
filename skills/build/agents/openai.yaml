interface:
  display_name: "build"
  short_description: "Implement the contract tree with TDD evidence"
  default_prompt: "Use $build to dispatch implementer subagents per contract, enforce RED/GREEN evidence, run merged per-contract reviews, retire mocks, and integrate. Requires isolated subagent support for full isolation; degrades honestly otherwise."
policy:
  allow_implicit_invocation: false
