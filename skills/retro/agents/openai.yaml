interface:
  display_name: "retro"
  short_description: "Weekly loop-engineering pass over ledgers"
  default_prompt: "Use $retro to mine feature ledgers and the error ledger for token distribution, rework routing, and recurring errors, then propose rule crystallization and process prunings for human approval."
policy:
  allow_implicit_invocation: true
