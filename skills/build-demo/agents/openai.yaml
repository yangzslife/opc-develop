interface:
  display_name: "build-demo"
  short_description: "Run the create-demo and review-demo improvement loop"
  default_prompt: "Use $build-demo to create, review, and iterate the demo until approved or blocked."
policy:
  allow_implicit_invocation: true
