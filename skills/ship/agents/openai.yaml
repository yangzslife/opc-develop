interface:
  display_name: "ship"
  short_description: "Release with evidence and finish the branch"
  default_prompt: "Use $ship to run release gates, confirm rollback readiness, deploy per project runbooks, smoke-check, and close out branches and worktrees safely."
policy:
  allow_implicit_invocation: false
