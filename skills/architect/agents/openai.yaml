interface:
  display_name: "architect"
  short_description: "Intake the PRD and produce the gated technical design"
  default_prompt: "Use $architect to pull the feature branch, run architecture intake, then write and gate technical.md with TD decision records, public contracts, and the runtime evidence plan."
policy:
  allow_implicit_invocation: true
