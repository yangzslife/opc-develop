interface:
  display_name: "acceptance-rework"
  short_description: "人工验收失败后的返工分诊"
  default_prompt: "使用 $acceptance-rework 处理人工验收不通过反馈，记录 human-acceptance.md，并路由到正确返工入口。"
policy:
  allow_implicit_invocation: true

