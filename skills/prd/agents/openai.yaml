interface:
  display_name: "prd"
  short_description: "Produce and gate the PRD, then push the handoff"
  default_prompt: "Use $prd to write prd.md with numbered PD decisions and AC-IDs from the approved demo, gate it, run the product sign-off, and push the feature branch for the architect."
policy:
  allow_implicit_invocation: true
