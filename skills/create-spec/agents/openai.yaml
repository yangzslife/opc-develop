interface:
  display_name: "create-spec"
  short_description: "Create an executable implementation spec"
  default_prompt: "Use $create-spec to create the implementation spec after technical design review is approved."
policy:
  allow_implicit_invocation: true
